# Self-Hosting RAGeval

1. **Installation:** Run `pip install omnismart-rageval`.
2. **Docker:** Deploy using the provided Dockerfile.
3. **Database:** Uses a local SQLite fallback (`sqlite:///rageval.db`) but Postgres is recommended for production.
